<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NotesApp</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input name="title" class="block px-5 py-3 w-full rounded-md outline-none bg-zinc-800" type="text" placeholder="Task Title...">
                <input name="details" class="block mt-2 px-5 py-3 w-full rounded-md outline-none bg-zinc-800 min-h-16" type="text" placeholder="Task Details...">
                <input class="mt-2 bg-blue-600 rounded-md px-4 py-2" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 text-white flex gap-3 flex-wrap">
            <% if(files.length > 0) { %>
                <%  files.forEach((task)=>{ %>
                    <div class="task bg-zinc-800 px-3 py-4 rounded-md min-w-72">
                        <h1 class="text-2xl font-semibold tracking-tighter"><%= task %></h1>
                        <div class="mt-3 flex justify-between items-center">
                            <a class="text-blue-500" href="/file/<%= task %>">Read More</a>
                            <a class="text-blue-500" href="/edit/<%= task %>">Edit Task</a>
                            <a class="text-red-500" href="">Delete</a>
                        </div>
                    </div>
                <%  })  %>
            <% } else{ %>
                <h3 class="text-zinc-600">No tasks yet.</h3>
            <% } %>
        </div>
    </div>
</body>
</html>